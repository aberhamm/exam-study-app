# MongoDB connection settings
# Copy this file to `.env.local` and update the values for your environment.
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=study-util
MONGODB_EXAMS_COLLECTION=exams
MONGODB_QUESTIONS_COLLECTION=questions
MONGODB_QUESTION_EMBEDDINGS_COLLECTION=question_embeddings

# Embeddings (for scripts/embed-questions.ts)
# Provide your OpenAI API key and model configuration
OPENAI_API_KEY=
QUESTIONS_EMBEDDING_MODEL=text-embedding-3-small
# QUESTIONS_EMBEDDING_DIMENSIONS=1536
# Vector index for embeddings collection
# MONGODB_QUESTION_EMBEDDINGS_VECTOR_INDEX=question_embedding

# Feature flags
# Enable development tools and endpoints even when NODE_ENV is not "development".
# Set to 1/true/yes/on to enable.
# Server-side toggle (affects API routes and middleware gating)
ENABLE_DEV_FEATURES=
# Client/UI toggle (read at build time for client-rendered pages)
NEXT_PUBLIC_ENABLE_DEV_FEATURES=

# (Note) Previously dev-only: /import, /dev/*, and APIs like /api/exams/:examId/search
